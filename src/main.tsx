import React,{useMemo,useState}from"react";import{createRoot}from"react-dom/client";import{Bell,LayoutDashboard,Plus,Search,Settings,Workflow}from"lucide-react";import"./style.css";const metrics=[["Connections","86"],["API calls","2.4M"],["Success","99.94%"],["Errors","1,482"]],rows=["CON-82 · Shop Desk → ERP · Orders · Healthy","CON-81 · ERP → Warehouse · Stock · Healthy","CON-80 · CRM → Ledgerly · Invoices · Warning"];function App(){const[q,setQ]=useState(""),[open,setOpen]=useState(false),list=useMemo(()=>rows.filter(x=>x.toLowerCase().includes(q.toLowerCase())),[q]);return <div className="app"><aside><h2>✦ Integrator</h2><button className="active"><LayoutDashboard/>Overview</button><button><Workflow/>Connections</button><button><Settings/>Settings</button><small>PLATFORM</small><p>API logs</p><p>Mappings</p><p>Health</p></aside><main><header>Integrations / Overview<div><Bell/><b>TJ</b></div></header><section><div className="hero"><div><small>MONDAY · SEPTEMBER 21</small><h1>Integrator</h1><p>Enterprise integrations and API operations</p></div><button className="primary" onClick={()=>setOpen(true)}><Plus/>New connection</button></div><div className="metrics">{metrics.map(m=><article><span>{m[0]}</span><strong>{m[1]}</strong><small>+8.4% this period</small></article>)}</div><article className="panel"><div className="head"><div><h3>Connection activity</h3><p>Monitor integrations and delivery health.</p></div><div className="search"><Search/><input placeholder="Search" value={q} onChange={e=>setQ(e.target.value)}/></div></div>{list.map(x=><div className="row"><b>{x.split(" · ")[0]}</b><span>{x.split(" · ").slice(1,-1).join(" · ")}</span><em>{x.split(" · ").at(-1)}</em></div>)}</article></section></main>{open&&<div className="modal"><div><h2>New connection</h2><p>Connect a service to your integration workspace.</p><input placeholder="Connection name"/><button className="primary" onClick={()=>setOpen(false)}>Create connection</button></div></div>}</div>}createRoot(document.getElementById("root")!).render(<React.StrictMode><App/></React.StrictMode>);